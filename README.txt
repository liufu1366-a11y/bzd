续写鸡 NEXT 一体化 v1.0.20

这是“全新的独立扩展”，不是旧续写鸡的补丁。

已内置：
1. 长篇编辑器 + SillyTavern 当前模型 generateRaw 续写
2. 动态 Prompt 模板：{{变量}}、{{#if}}、{{#unless}}、<%= var %>
3. ScriptEvents 式内置事件总线 + SillyTavern 事件桥
4. 内置关键词触发世界书
5. 长期记忆 / 手动总结 / 自动总结
6. RP 世界状态
7. DAMAGE / REPAIR 连续状态
8. 电子眼 / 机体 HUD
9. 自检面板和事件日志
10. 每聊天/角色作用域数据隔离
11. 世界书/记忆/完整状态 JSON 导入导出

安装：
- 把整个 XuXieJi_Next 文件夹放进 SillyTavern/public/scripts/extensions/third-party/
- 或将 ZIP 用 SillyTavern 的扩展安装方式导入（取决于你使用的前端版本）。
- 刷新酒馆，打开“扩展”设置，找到“续写鸡 NEXT 一体化”。

重要：
- 不需要旧续写鸡。
- 不需要单独安装 ST-Prompt-Template。
- 不需要单独安装 Extension-ScriptEvents。
- 动态 Prompt、事件总线、世界书、RP/DAMAGE/HUD 都是插件自身代码。


v1.0.20：修复手机端编辑器弹窗大面积黑屏、标题和按钮不易看到的问题。


v1.0.20：长篇编辑器改为独立全屏页面，不再使用弹窗布局；修复手机端只出现黑块或一条线的问题。


v1.0.20：世界书、长期记忆、RP/DAMAGE、动态Prompt、自检/日志全部改为全屏管理页，顶部固定“← 返回”，主体独立滚动。


v1.0.20：
- 修复续写脱离上文：加入“连续正文上下文 + 续写锚点 + 当前最后一句”三层连续性约束，默认送入最近 30000 字。
- 新增“默认每次续写字数”和编辑器内“本次字数”，生成时同时写入长度要求并设置 max_tokens 上限。
- “世界书”改为 SillyTavern 原生 World Info/Lorebook：按钮列出酒馆真实世界书并调用原生编辑器。
- 生成时调用酒馆原生 getWorldInfoPrompt 扫描当前激活世界书，不再依赖续写鸡自制的简化关键词世界书。


v1.0.20：
- 通用核心彻底取消《变身机娘》硬编码注入；升级时自动关闭旧 RP/DAMAGE 专用注入，并清理可识别的苏若/SURO 旧默认状态。
- DAMAGE 核心不再写死机体/电子眼规则；特定作品规则应由对应世界书/项目预设提供。
- 新增酒馆大厅常驻悬浮“续写鸡”入口，手机端显示为 ✦ 圆形按钮，直接打开长篇编辑器，无需每次进入扩展设置。


v1.0.20：
- 大厅悬浮“续写鸡”按钮支持手指/鼠标自由拖动。
- 拖到哪里就停在哪里，并自动记住位置；刷新 SillyTavern 后仍恢复上次位置。
- 拖动和轻点已区分，拖动结束不会误打开编辑器。
- 屏幕旋转或窗口尺寸变化时会自动把按钮限制在可见区域内。


v1.0.20：
- 新增 TXT 导入；导入后保存一份完整原书备份。
- 新增章节识别与“章节定位”，可以选择任意章节并从该章末尾开始续写。
- 新增“恢复导入原文”，随时回到最近一次导入的完整小说。
- 新增背景/字体：深色、护眼米黄、纸张白、深灰；可调字体、字号、行距。
- 通用核心彻底改为中性动态状态，不再预置任何特定作品、角色、机体或电子眼设定。
- 升级时清理旧版专用 RP/DAMAGE 状态，并过滤明显由旧专用预设污染的长期记忆。


v1.0.20：
- 改为“真正的手动保存”。只有点击编辑器右上角“保存”才会写入正式正文。
- 开始续写后生成的新内容默认只在当前编辑器里，不再自动保存。
- 手动输入、TXT导入、章节定位、恢复导入原文也都不会自动覆盖正式正文。
- 有未保存修改时点“返回”会提示；确认丢弃后恢复最后一次正式保存内容。
- 标题字数旁显示“未保存 / 已保存”状态。


v1.0.20：
- 每次生成后，在编辑器旧正文与本次新内容之间显示明显分界线。
- 分界线仅用于辨认，不会保存进正文、不会导出、不会送给 AI。
- 编辑器顶部常驻显示：动态Prompt / 事件监听 / 原生世界书 三项状态。
- ● 表示当前可用/启用，○ 表示关闭或未连接；点击状态可进入详细自检。


v1.0.20：
- 内置“机械少女（机娘）完整设定 V1.2”世界书预设，共 27 个条目。
- 世界书内容来自用户提供的《机械少女（机娘）完整设定文档 V1.2 酒馆版》。
- 在“世界书”页面可一键安装到 SillyTavern 原生 World Info。
- 安装后默认不强制启用；由用户手动启用/停用，避免污染其它小说。
- 可以直接打开 SillyTavern 原生世界书编辑器查看和修改全部条目。
- 编辑器状态条新增“机娘V1.2”状态，直接显示这本世界书是否正在参与续写。

v1.0.20：
- 修复机娘 V1.2 原生世界书安装后显示 0 条目的问题，并在保存后强制校验词条数量。
- 新增“词条目录”：直接在续写鸡里浏览 27 个内置词条，不必等待酒馆重型世界书编辑器。
- 新增长篇编辑器“故事”项目管理：新建、切换、改名、删除；每个故事独立保存正式正文和导入原书。
- 当前故事名称直接显示在编辑器标题下。
- 优化大正文输入：不再每次按键都重写整个 textarea，降低手机卡顿。
- “总结正文”增加处理中状态提示。

v1.0.20：
- 修复“词条目录 HTTP 404”：27 条机娘 V1.2 世界书数据直接内嵌到 index.js，不再 fetch assets 文件。
- 世界书页面显示真实词条数量；空世界书会出现“修复空世界书 / 写入 27 条”。
- 顶部按钮改用 pointerup 快速触发，并先显示“打开中/扫描中/导出中”等反馈。
- 移除编辑器打开时自动 focus，减少 Android WebView/输入法造成的触控卡顿。
- renderEditor 不再隐式重写整篇大 textarea，避免长文本下点击按钮时卡顿。

v1.0.20：
- “故事”改成编辑器内直接下拉切换，不再弹出故事管理大页面；旁边可快速“＋故事”。
- 新增可编辑的轻量机娘世界书：名称、关键词、常驻、停用、正文均可修改。
- 机娘世界书生成时只取 1 个常驻 + 最多 3 个命中词条，总量最多约 5000 字符，不整本注入。
- 正文上下文默认从 30000 字符降为 12000 字符，减少 token；仍保留末尾续写锚点和长期记忆。
- 编辑器直接显示当前省 Token 策略。

v1.0.20：
- 新增“数据库”按钮：每个故事拥有独立的长篇检索数据库。
- 导入 TXT 后自动把原小说切成约 1800 字符片段并建立本地索引。
- 续写时按最近正文、最后一句和定向要求检索旧剧情；最多取 5 个相关片段，总量最多约 7500 字符。
- 不会把整本 100 万/150 万字小说每次全部发送给生成模型。
- 数据库可以手动重新建立或清空；不会删除正文。
- 这是无需 Embedding 的续写鸡轻量 RAG；SillyTavern 原生 Data Bank / Vector Storage 仍可并行使用。

v1.0.20：
- 长期记忆专门保存压缩剧情摘要（最多约 4200 字符）。
- 数据库专门检索旧剧情原文（最多约 7500 字符）。
- 两者高度重复时减少重复摘要，避免双份浪费 Token。
- 最近正文仍是当前剧情的最高依据；数据库旧片段仅用于回忆历史。

v1.0.20 卡死专项修复：
- 日志不再每个事件都同步写 localStorage，也不会在诊断页未打开时反复重绘。
- 世界书不再一次创建 27 个 textarea；目录只有轻量按钮，点某词条才创建一个编辑框。
- 章节列表最多一次渲染 300 条，并支持筛选，避免超长小说生成几千个 DOM 节点。
- 数据库长篇索引改为分批异步建立，约每 12ms 主动让出一次 UI 线程，并显示建立进度。
- 新增“轻量模式”，可隐藏非必要状态条，降低手机 WebView 重绘负担。

v1.0.20：
- 修复正文已经有 1.6 万字但顶部仍显示 0 字：字数现在直接读取 textarea 实际内容，不再依赖旧 editorText 状态。
- 输入时只用 requestAnimationFrame 更新数字，不复制/重绘整篇正文。
- 保存后立即把状态切为“已保存”并显示实际字数。
- 撤销 pointerup + click 抑制方案；Android WebView 顶部功能按钮统一恢复标准 click，解决故事能用但其他按钮失效的问题。
- 所有编辑器功能按钮显式 type=button，避免移动 WebView 的默认按钮行为干扰。

v1.0.20 根因修复：
- 找到“故事能用但其他按钮全失效”的真正原因：状态条使用了不存在的 eventBus 变量，打开编辑器时抛 ReferenceError，导致后续按钮绑定代码完全没有执行。
- 状态条改用实际的 stBridgeReady，并增加 try/catch；以后状态检测出错也不会拖死整个编辑器。
- 找到字数显示的第二个错误：renderEditor 里残留了不存在的 wc 变量并再次读取旧 editorText。已彻底删除。
- 正文字数现在只读取 textarea 实际 value；导入、输入、粘贴、输入法 compositionend 都会刷新。
- 把状态条渲染移动到所有核心按钮绑定之后，确保可选模块故障不会再让按钮全部失效。
- 标题直接显示当前版本号 v1.0.20，方便确认酒馆实际加载的是不是新版。

v1.0.20：
- 修复浅色/护眼背景下标题、副标题、字数颜色过淡的问题，强制使用高对比度文字。
- 正文编辑区默认高度提高到手机约 62vh，桌面约 58vh。
- 新增“正文全屏”按钮，可隐藏大部分工具区，把正文扩大到手机约 76vh。
- 新增独立的新内容分界线显示区，以及“新内容”按钮用于跳到本次生成内容起点。
- 生成后会刷新新内容分界状态。

v1.0.20 按钮初始化专项：
- 根据“故事能点、其余都不能点”的执行顺序，锁定故障点在故事绑定之后、功能按钮绑定之前。
- 外观初始化 applyEditorAppearance 改为完全隔离；主题/字体哪怕报错也不能再阻断按钮。
- 所有功能按钮改成最普通标准 click，不再 blur、pointerup 或依赖动画帧。
- getSettings、renderEditor、updateBusy、状态条全部降级隔离，任何可选模块异常都不能杀掉核心按钮。
- 标题下新增“按钮已绑定”状态；看到它说明 openEditor 已完整执行到底。
- 加入核心按钮事件委托兜底。
